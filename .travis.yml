language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version 
  - bash build-images.sh

# Tasks to perform after the process is successful.
after_success:
  - bash tag-and-push-images.sh